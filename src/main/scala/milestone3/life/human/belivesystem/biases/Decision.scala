package milestone3.life.human.belivesystem.biases

/**
 * Decision class:
 * The decision class represents a decision-making process.
 * It contain attributes such as options, criteria, and the selected option.
 */
case class Decision(options: List[String], selectedOption: String)
